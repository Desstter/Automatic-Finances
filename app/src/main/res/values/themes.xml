<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AutomaticFinances" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Translucent host for the voice-entry overlay launched from the notification.
         Lets the Compose bottom-sheet float over whatever was on screen. -->
    <style name="Theme.AutomaticFinances.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>
</resources>